<%= form_for @user, :url => '/index/email_sender', :html => { :multipart => true } do |form| %>
  <%= form.file_field :avatar %>
<% end %>

<%= image_tag @user.avatar.url %>
<%= image_tag @user.avatar.url(:medium) %>
<%= image_tag @user.avatar.url(:thumb) %>